module.exports = {
    name:'maintenance',
    usage:'maintenance',
    async execute(){
        // The code for this command is in app.js, this file exists just to have the other command properties.
        return true;
    }
};